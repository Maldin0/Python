"""I'm dying"""
def chick(num):
    """Is is 60?"""
    if 50 <= num <= 70:
        return 1
    return 0
def main():
    """count it!!"""
    last = 0
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    last += chick(float(input()))
    print(last)
main()
